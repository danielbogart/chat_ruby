%h4 Please feel free to add categories that you think would be helpful

.col-sm-4
  .list-group
    %ul
    - @categories.each do |category|
      %li.list-group-item= link_to category.title, category_resources_path(category)

    = render "categories/form"


