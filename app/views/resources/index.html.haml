%h1= @category.title

.col-sm-4
  .list-group
    %ul
    - @resources.each do |resource|
      %li.list-group-item= link_to resource.name, resource.url
      %p.list-group-item= resource.description

    = render "resources/form"
