= form_for [@category, @resource] do |f|
  %p
    = f.label :name
    = f.text_field :name
    = f.label :description
    = f.text_area :description
    = f.label :url
    = f.text_field :url
    / = f.hidden_field_tag :user_id, current_user.id
    = f.submit "Create new resource!"